# Desktop app environment (optional overrides).
# API keys live in the repo-root .env; this file is only for UI-side overrides.

# Ports of the local Python servers the app launches and talks to.
# SENSING_PORT="8080"
# TUTOR_PORT="8081"

# Start the avatar minimized on launch.
# START_MINIMIZED="1"
